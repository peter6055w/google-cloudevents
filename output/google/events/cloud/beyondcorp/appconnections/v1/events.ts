// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               v6.32.0
// source: google/events/cloud/beyondcorp/appconnections/v1/events.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { AppConnectionEventData } from "./data";

export const protobufPackage = "google.events.cloud.beyondcorp.appconnections.v1";

/** The CloudEvent raised when an AppConnection is created. */
export interface AppConnectionCreatedEvent {
  /** The data associated with the event. */
  data?: AppConnectionEventData | undefined;
}

/** The CloudEvent raised when an AppConnection is updated. */
export interface AppConnectionUpdatedEvent {
  /** The data associated with the event. */
  data?: AppConnectionEventData | undefined;
}

/** The CloudEvent raised when an AppConnection is deleted. */
export interface AppConnectionDeletedEvent {
  /** The data associated with the event. */
  data?: AppConnectionEventData | undefined;
}

function createBaseAppConnectionCreatedEvent(): AppConnectionCreatedEvent {
  return { data: undefined };
}

export const AppConnectionCreatedEvent: MessageFns<AppConnectionCreatedEvent> = {
  encode(message: AppConnectionCreatedEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.data !== undefined) {
      AppConnectionEventData.encode(message.data, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AppConnectionCreatedEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAppConnectionCreatedEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data = AppConnectionEventData.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AppConnectionCreatedEvent {
    return { data: isSet(object.data) ? AppConnectionEventData.fromJSON(object.data) : undefined };
  },

  toJSON(message: AppConnectionCreatedEvent): unknown {
    const obj: any = {};
    if (message.data !== undefined) {
      obj.data = AppConnectionEventData.toJSON(message.data);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AppConnectionCreatedEvent>, I>>(base?: I): AppConnectionCreatedEvent {
    return AppConnectionCreatedEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AppConnectionCreatedEvent>, I>>(object: I): AppConnectionCreatedEvent {
    const message = createBaseAppConnectionCreatedEvent();
    message.data = (object.data !== undefined && object.data !== null)
      ? AppConnectionEventData.fromPartial(object.data)
      : undefined;
    return message;
  },
};

function createBaseAppConnectionUpdatedEvent(): AppConnectionUpdatedEvent {
  return { data: undefined };
}

export const AppConnectionUpdatedEvent: MessageFns<AppConnectionUpdatedEvent> = {
  encode(message: AppConnectionUpdatedEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.data !== undefined) {
      AppConnectionEventData.encode(message.data, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AppConnectionUpdatedEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAppConnectionUpdatedEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data = AppConnectionEventData.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AppConnectionUpdatedEvent {
    return { data: isSet(object.data) ? AppConnectionEventData.fromJSON(object.data) : undefined };
  },

  toJSON(message: AppConnectionUpdatedEvent): unknown {
    const obj: any = {};
    if (message.data !== undefined) {
      obj.data = AppConnectionEventData.toJSON(message.data);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AppConnectionUpdatedEvent>, I>>(base?: I): AppConnectionUpdatedEvent {
    return AppConnectionUpdatedEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AppConnectionUpdatedEvent>, I>>(object: I): AppConnectionUpdatedEvent {
    const message = createBaseAppConnectionUpdatedEvent();
    message.data = (object.data !== undefined && object.data !== null)
      ? AppConnectionEventData.fromPartial(object.data)
      : undefined;
    return message;
  },
};

function createBaseAppConnectionDeletedEvent(): AppConnectionDeletedEvent {
  return { data: undefined };
}

export const AppConnectionDeletedEvent: MessageFns<AppConnectionDeletedEvent> = {
  encode(message: AppConnectionDeletedEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.data !== undefined) {
      AppConnectionEventData.encode(message.data, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AppConnectionDeletedEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAppConnectionDeletedEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data = AppConnectionEventData.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AppConnectionDeletedEvent {
    return { data: isSet(object.data) ? AppConnectionEventData.fromJSON(object.data) : undefined };
  },

  toJSON(message: AppConnectionDeletedEvent): unknown {
    const obj: any = {};
    if (message.data !== undefined) {
      obj.data = AppConnectionEventData.toJSON(message.data);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AppConnectionDeletedEvent>, I>>(base?: I): AppConnectionDeletedEvent {
    return AppConnectionDeletedEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AppConnectionDeletedEvent>, I>>(object: I): AppConnectionDeletedEvent {
    const message = createBaseAppConnectionDeletedEvent();
    message.data = (object.data !== undefined && object.data !== null)
      ? AppConnectionEventData.fromPartial(object.data)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
