// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               v6.32.0
// source: google/events/cloud/storage/v1/events.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { StorageObjectData } from "./data";

export const protobufPackage = "google.events.cloud.storage.v1";

/** The CloudEvent raised when an object is finalized in Google Cloud Storage. */
export interface ObjectFinalizedEvent {
  /** The data associated with the event. */
  data?: StorageObjectData | undefined;
}

/** The CloudEvent raised when an object is archived in Google Cloud Storage. */
export interface ObjectArchivedEvent {
  /** The data associated with the event. */
  data?: StorageObjectData | undefined;
}

/** The CloudEvent raised when an object is deleted  in Google Cloud Storage. */
export interface ObjectDeletedEvent {
  /** The data associated with the event. */
  data?: StorageObjectData | undefined;
}

/**
 * The CloudEvent raised when an object's metadata is updated  in Google Cloud
 * Storage.
 */
export interface ObjectMetadataUpdatedEvent {
  /** The data associated with the event. */
  data?: StorageObjectData | undefined;
}

function createBaseObjectFinalizedEvent(): ObjectFinalizedEvent {
  return { data: undefined };
}

export const ObjectFinalizedEvent: MessageFns<ObjectFinalizedEvent> = {
  encode(message: ObjectFinalizedEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.data !== undefined) {
      StorageObjectData.encode(message.data, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ObjectFinalizedEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseObjectFinalizedEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data = StorageObjectData.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ObjectFinalizedEvent {
    return { data: isSet(object.data) ? StorageObjectData.fromJSON(object.data) : undefined };
  },

  toJSON(message: ObjectFinalizedEvent): unknown {
    const obj: any = {};
    if (message.data !== undefined) {
      obj.data = StorageObjectData.toJSON(message.data);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ObjectFinalizedEvent>, I>>(base?: I): ObjectFinalizedEvent {
    return ObjectFinalizedEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ObjectFinalizedEvent>, I>>(object: I): ObjectFinalizedEvent {
    const message = createBaseObjectFinalizedEvent();
    message.data = (object.data !== undefined && object.data !== null)
      ? StorageObjectData.fromPartial(object.data)
      : undefined;
    return message;
  },
};

function createBaseObjectArchivedEvent(): ObjectArchivedEvent {
  return { data: undefined };
}

export const ObjectArchivedEvent: MessageFns<ObjectArchivedEvent> = {
  encode(message: ObjectArchivedEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.data !== undefined) {
      StorageObjectData.encode(message.data, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ObjectArchivedEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseObjectArchivedEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data = StorageObjectData.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ObjectArchivedEvent {
    return { data: isSet(object.data) ? StorageObjectData.fromJSON(object.data) : undefined };
  },

  toJSON(message: ObjectArchivedEvent): unknown {
    const obj: any = {};
    if (message.data !== undefined) {
      obj.data = StorageObjectData.toJSON(message.data);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ObjectArchivedEvent>, I>>(base?: I): ObjectArchivedEvent {
    return ObjectArchivedEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ObjectArchivedEvent>, I>>(object: I): ObjectArchivedEvent {
    const message = createBaseObjectArchivedEvent();
    message.data = (object.data !== undefined && object.data !== null)
      ? StorageObjectData.fromPartial(object.data)
      : undefined;
    return message;
  },
};

function createBaseObjectDeletedEvent(): ObjectDeletedEvent {
  return { data: undefined };
}

export const ObjectDeletedEvent: MessageFns<ObjectDeletedEvent> = {
  encode(message: ObjectDeletedEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.data !== undefined) {
      StorageObjectData.encode(message.data, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ObjectDeletedEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseObjectDeletedEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data = StorageObjectData.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ObjectDeletedEvent {
    return { data: isSet(object.data) ? StorageObjectData.fromJSON(object.data) : undefined };
  },

  toJSON(message: ObjectDeletedEvent): unknown {
    const obj: any = {};
    if (message.data !== undefined) {
      obj.data = StorageObjectData.toJSON(message.data);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ObjectDeletedEvent>, I>>(base?: I): ObjectDeletedEvent {
    return ObjectDeletedEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ObjectDeletedEvent>, I>>(object: I): ObjectDeletedEvent {
    const message = createBaseObjectDeletedEvent();
    message.data = (object.data !== undefined && object.data !== null)
      ? StorageObjectData.fromPartial(object.data)
      : undefined;
    return message;
  },
};

function createBaseObjectMetadataUpdatedEvent(): ObjectMetadataUpdatedEvent {
  return { data: undefined };
}

export const ObjectMetadataUpdatedEvent: MessageFns<ObjectMetadataUpdatedEvent> = {
  encode(message: ObjectMetadataUpdatedEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.data !== undefined) {
      StorageObjectData.encode(message.data, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ObjectMetadataUpdatedEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseObjectMetadataUpdatedEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data = StorageObjectData.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ObjectMetadataUpdatedEvent {
    return { data: isSet(object.data) ? StorageObjectData.fromJSON(object.data) : undefined };
  },

  toJSON(message: ObjectMetadataUpdatedEvent): unknown {
    const obj: any = {};
    if (message.data !== undefined) {
      obj.data = StorageObjectData.toJSON(message.data);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ObjectMetadataUpdatedEvent>, I>>(base?: I): ObjectMetadataUpdatedEvent {
    return ObjectMetadataUpdatedEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ObjectMetadataUpdatedEvent>, I>>(object: I): ObjectMetadataUpdatedEvent {
    const message = createBaseObjectMetadataUpdatedEvent();
    message.data = (object.data !== undefined && object.data !== null)
      ? StorageObjectData.fromPartial(object.data)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
